apiVersion: v1
kind: Service
metadata:
  name: affine
spec:
  type: NodePort
  ports:
    - port: {{ .Values.app.port }}
      targetPort: {{ .Values.app.port }}
      nodePort: {{ .Values.app.nodePort }}
  selector:
    app: affine
